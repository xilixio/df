config:
  description: "Example packages.yaml for testing."
packages:
  pk1:
    deps:
      - pk2
      - pk3
    Linux:
      install: "sh pk1.sh"
      check: "[ -e /tmp/pk1.tmp ]"
    Darwin:
      install: "sh pk1.sh"
      check: "[ -e /tmp/pk1.tmp ]"
  pk2:
    deps:
      - pk3
    Linux:
      install: "sh pk2.sh"
      check: "[ -e /tmp/pk2.tmp ]"
    Darwin:
      install: "sh pk2.sh"
      check: "[ -e /tmp/pk2.tmp ]"
  pk3:
    deps:
    Linux:
      install: "sh pk3.sh"
      check: "[ -e /tmp/pk3.tmp ]"
  pk_no_os:
    Something: "tbd"
  pk_no_check:
    Linux: "tbd"
    Darwin: "tbd"